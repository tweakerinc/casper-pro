[platformio]
; Casper Pro targets Xteink X4 Pro (ESP32-S3). C3 envs remain for shared code paths.
default_envs = x4pro
build_cache_dir = .cache
extra_configs = platformio.local.ini

[casper]
; Product version — bump this before each release; match the GitHub tag (v0.1.9, …).
; scripts/git_branch.py injects CASPER_VERSION for env:default.
; gh_release post-script copies firmware to dist/Casper-<version>.bin (e.g. Casper-v0.1.9.bin).
; Product tag must match GitHub release tag (v0.1.9). After shipping, bump here
; for the next build train and create a new tag — never ship a bin without
; pushing that commit + tag to casper remote first (Source code zip uses the tag).
version = v0.1.9
product = Casper Pro

[base]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = esp32-c3-devkitm-1
framework = arduino
monitor_speed = 115200
upload_speed = 921600
check_tool = cppcheck
; missingInclude (project headers) is suppressed alongside missingIncludeSystem: on a
; fresh CI checkout cppcheck has no resolved include paths, so it reports every
; project header as missing (~400 information-level lines) and fails the job.
; Fail-on-defect low is enabled in CI. Style nits that do not affect firmware
; quality (STL rewrite suggestions, false-positive always-false on static flags)
; are suppressed so the job tracks real defects.
check_flags =
  --enable=all
  --suppress=missingIncludeSystem
  --suppress=missingInclude
  --suppress=unusedFunction
  --suppress=unusedPrivateFunction
  --suppress=useStlAlgorithm
  --suppress=knownConditionTrueFalse
  --suppress=unmatchedSuppression
  --suppress=*:*/.pio/*
  --inline-suppr
check_skip_packages = yes

; Active theme chain: BaseTheme → LyraTheme → MinimalTheme → Bare / Penumbra only.
; Dead themes/fonts/classic reader removed from the tree (not merely filtered).
build_src_filter =
  +<*>

board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_upload.offset_address = 0x10000

build_flags =
  -DARDUINO_USB_MODE=1
  -DARDUINO_USB_CDC_ON_BOOT=1
  -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
  -DDISABLE_FS_H_WARNING=1
  -DDESTRUCTOR_CLOSES_FILE=1
# Casper product firmware identity.
  -DCASPER_PRODUCT_FIRMWARE=1
# Rivulet is the only EPUB reader in product firmware.
  -DCASPER_RIVULET_READER=1
# https://libexpat.github.io/doc/api/latest/#XML_GE
  -DXML_GE=0
  -DXML_CONTEXT_BYTES=1024
  -std=gnu++2a
# Enable UTF-8 long file names in SdFat
  -DUSE_UTF8_LONG_NAMES=1
# PNG scanline buffer (current+previous row). Was 16416 for 2048px RGBA, which
# inflated sizeof(PNG) to ~59KB — Alice paint-time maxAlloc is often 36–49KB so
# `new PNG()` always failed and the drop-cap "A" never rendered. 8200 covers
# ~1024px RGBA / ~4k grayscale (beyond the 528px panel); wider sources fail the
# requiredInternal check cleanly. Saves ~8KB contiguous for the decoder object.
  -DPNG_MAX_BUFFERED_PIXELS=8200
# Casper OTA: GitHub Releases API (override if the repo moves)
  -DCASPER_OTA_RELEASE_URL=\"https://api.github.com/repos/TweakerInc/casper/releases/latest\"
  -DFREEINK_NET_WOLFSSL=1
  -DWOLFSSL_USER_SETTINGS
  -DWOLFSSL_OPTIONS_H
  -DWOLFSSL_CLIENT_EXAMPLE
  -DWOLFSSL_TLS13
# MEMFIX-PORT: single-precision ECC (sp_c32.c). Without it every P-256 operation
# (TLS 1.3 key_share keygen, ECDHE, ECDSA cert verify) runs on fast-math bignums
# that WOLFSSL_SMALL_STACK heap-allocates at FP_MAX_BITS size -- tens of KB of
# temporaries, which OOMs (MP_MEM) at the ~50KB free heap a reading session
# leaves. SP uses fixed 256-bit arrays: a few KB, and several times faster.
# SP_SMALL trades the large precomputed point tables for smaller flash.
  -DWOLFSSL_HAVE_SP_ECC
  -DWOLFSSL_SP_SMALL
  -DHAVE_TLS_EXTENSIONS
  -DHAVE_SUPPORTED_CURVES
  -DHAVE_HKDF
  -DHAVE_FFDHE_2048
  -DHAVE_CURVE25519
  -DWC_RSA_PSS
  -DHAVE_SNI
  -Wno-bidi-chars
  -Wl,--wrap=panic_print_backtrace,--wrap=panic_abort,--wrap=bootloader_common_check_efuse_blk_validity
  -fno-exceptions

build_unflags =
  -std=gnu++11
  -fexceptions

; Board configuration
board_build.flash_mode = dio
board_build.flash_size = 16MB
board_build.partitions = partitions.csv

; MEMFIX-PORT: custom_sdkconfig heap reclamation (~32-37 KB). Rebuilds the
; Arduino core libs on first build (slower once, cached after; needs the CMake
; pin in platformio.local.ini on macOS).
;
; If an interrupted rebuild fails with "multiple definition of 'app_main'"
; (stale generated scaffold), clean it up with:
;   rm -rf .dummy CMakeLists.txt sdkconfig.default sdkconfig.defaults .pio/build/default
; Do NOT use `git clean -fdX` — it deletes platformio.local.ini.
custom_sdkconfig =
  ; --- Bluetooth disabled (Casper) -------------------------------------------
  ; BLE page-turner support was removed from the product build after repeated
  ; connect-time OOM aborts on the C3. Keep the controller out of the core so
  ; we reclaim heap/IRAM. Re-enable only with a full memory plan + NimBLE host.
  CONFIG_BT_ENABLED=n
  CONFIG_BT_CONTROLLER_ENABLED=n
  CONFIG_BT_BLUEDROID_ENABLED=n
  CONFIG_BT_NIMBLE_ENABLED=n
  CONFIG_BLE_MESH=n
  ; Arduino loop stack (WiFi/TLS still deep).
  CONFIG_ARDUINO_LOOP_STACK_SIZE=16384
  ; Task stack right-sizing from measured high-water marks (heap block map +
  ; per-task stack audit, July 2026): esp_timer used ~0.8 KB of 8 KB across
  ; every capture; the FreeRTOS timer service used ~0.5 KB
  ; of 4 KB. Neither runs TLS or app code. ~7 KB back to the heap.
  CONFIG_ESP_TIMER_TASK_STACK_SIZE=4096
  CONFIG_FREERTOS_TIMER_TASK_STACK_DEPTH=2560
  ; Move the WiFi stack's non-critical hot paths out of IRAM into flash.
  ; On the C3, IRAM and DRAM share one SRAM pool, so the ~25-30 KB this
  ; frees lands directly in the heap — paid for with lower WiFi throughput
  ; during transfers (occasional sync/OTA use, not streaming: acceptable).
  ; IRAM cost is static, so the heap gain applies even with WiFi off.
  CONFIG_ESP_WIFI_IRAM_OPT=n
  CONFIG_ESP_WIFI_RX_IRAM_OPT=n
  ; Keep the Arduino wrappers for the removed cloud components (below) out of
  ; the core source list; all other bundled libraries default to enabled.
  CONFIG_ARDUINO_SELECTIVE_COMPILATION=y
  CONFIG_ARDUINO_SELECTIVE_RainMaker=n
  CONFIG_ARDUINO_SELECTIVE_Insights=n
  ; ESP_SR (speech recognition) only exists in the S3 core; its wrapper includes
  ; ESP_I2S.h, which the isolated core rebuild can't resolve. Unused here anyway,
  ; so drop it or the sticky env fails to build.
  CONFIG_ARDUINO_SELECTIVE_ESP_SR=n

; Drop unused cloud components from the core rebuild. esp_insights/rainmaker
; require embedded server certs the lib builder can't generate
; ("https_server.crt.S not found"); this firmware uses none of them.
custom_component_remove =
  espressif/esp_insights
  espressif/esp_rainmaker
  espressif/esp_diagnostics
  espressif/esp_diag_data_store
  espressif/esp_schedule
  espressif/esp_rcp_update
  espressif/esp_secure_cert_mgr
  espressif/cbor

extra_scripts =
  pre:scripts/patch_wolfssl.py
  pre:scripts/build_html.py
  pre:scripts/gen_i18n.py
  pre:scripts/git_branch.py
  pre:scripts/patch_jpegdec.py
  post:scripts/register_unit_tests_target.py

; Libraries
lib_deps =
  BatteryMonitor=symlink://freeink-sdk/libs/hardware/BatteryMonitor
  InputManager=symlink://freeink-sdk/libs/hardware/InputManager
  EInkDisplay=symlink://freeink-sdk/libs/display/FreeInkDisplay
  SDCardManager=symlink://freeink-sdk/libs/hardware/SDCardManager
  BoardConfig=symlink://freeink-sdk/libs/hardware/BoardConfig
  XteinkDetect=symlink://freeink-sdk/libs/hardware/XteinkDetect
  PowerManager=symlink://freeink-sdk/libs/hardware/PowerManager
  Rtc=symlink://freeink-sdk/libs/hardware/Rtc
  Imu=symlink://freeink-sdk/libs/hardware/Imu
  FrontlightManager=symlink://freeink-sdk/libs/hardware/FrontlightManager
  SecureNet=symlink://freeink-sdk/libs/network/SecureNet
  ; BleKeyboardHost / NimBLE not linked — Bluetooth page-turner removed from product.
  FreeInkUI=symlink://freeink-sdk/libs/ui/FreeInkUI
  Icons=symlink://freeink-sdk/libs/assets/Icons
  bblanchon/ArduinoJson @ 7.4.2
  ricmoo/QRCode @ 0.0.1
  bitbank2/PNGdec @ 1.1.6
  https://github.com/bitbank2/JPEGDEC.git#86282979224c8a32fd51e091ed5a35b0c699a52b
  links2004/WebSockets @ 2.7.3
  wolfssl/Arduino-wolfSSL @ 5.7.2

lib_ignore =
  BLE
  NimBLE-Arduino

; Version string macros for release envs (default uses scripts/git_branch.py).
; CASPER_VERSION is the product version string.
casper_version_flags =
  -DCASPER_VERSION=\"${casper.version}\"

[env:default]
extends = base
build_flags =
  ${base.build_flags}
  ; CASPER_VERSION is set by scripts/git_branch.py from [casper] version
  -DFREEINK_DEVICE_X4=1
  -DFREEINK_DEVICE_X3=1
  ; Bluetooth / BLE HID host: OFF (no controller, no NimBLE, no UI).
  -DFREEINK_CAP_BLE_HID_HOST=0
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2 ; Set log level to debug for development builds


; One concurrent firmware for Xteink X3 + X4 (runtime BoardConfig select).
; UI chrome (Dashboard shell, double rules, side/front nav) is shared; only
; hardware-backed features gate on gpio.deviceIsX3() (RTC, IMU, fuel gauge, etc.).
[env:gh_release]
extends = base
; Must keep ${base.extra_scripts}: child extra_scripts *replaces* parent, so without
; this gen_i18n / wolfssl patch / etc. never run. Local trees often still have
; generated I18n from a prior `default` build; clean CI checkouts do not → fail.
extra_scripts =
  ${base.extra_scripts}
  post:scripts/copy_release_firmware.py
build_flags =
  ${base.build_flags}
  ${base.casper_version_flags}
  -DFREEINK_DEVICE_X4=1
  -DFREEINK_DEVICE_X3=1
  -DFREEINK_CAP_BLE_HID_HOST=0
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release builds

[env:gh_release_rc]
extends = base
build_flags =
  ${base.build_flags}
  -DCASPER_VERSION=\"${casper.version}-rc+${sysenv.CASPER_RC_HASH}\"
  -DFREEINK_DEVICE_X4=1
  -DFREEINK_DEVICE_X3=1
  -DFREEINK_CAP_BLE_HID_HOST=0
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release candidate builds

[env:slim]
extends = base
build_flags =
  ${base.build_flags}
  -DCASPER_VERSION=\"${casper.version}-slim\"
  -DFREEINK_DEVICE_X4=1
  -DFREEINK_DEVICE_X3=1
  -DFREEINK_CAP_BLE_HID_HOST=0
  ; serial output is disabled in slim builds to save space
  -UENABLE_SERIAL_LOG

; --- Xteink X4 Pro — ESP32-S3 + 8MB PSRAM, 800x480 + GT911 + warm/cold light ---
; Distinct from C3 X4. Board profile: freeink-sdk docs/xteink-x4pro-support.md
; CAP_TOUCH + CAP_FRONTLIGHT + CAP_WARMLIGHT + SDMMC auto-enable.
;   pio run -e x4pro
;   pio run -e x4pro -t upload
[env:x4pro]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
build_flags =
  ${base.build_flags}
  -DCASPER_VERSION=\"${casper.version}-pro\"
  -DBOARD_HAS_PSRAM
  -DFREEINK_DEVICE_X4PRO=1
  -DUSE_BLOCK_DEVICE_INTERFACE=1
  -DFREEINK_CAP_BLE_HID_HOST=0
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2

; --- Seeed Sticky — ESP32-S3R8, 3.97" 800x480 SSD1677 + GT911 touch -----------
; Different MCU family than the C3 envs (one binary per family). The SDK
; auto-enables CAP_TOUCH (GT911) and the BQ27220 gauge for this device; PSRAM is
; intentionally left off (48KB framebuffer fits in DRAM, same as X4).
;   pio run -e sticky -t upload
[env:sticky]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
build_flags =
  ${base.build_flags}
  -DCASPER_VERSION=\"${casper.version}-sticky\"
  -DFREEINK_DEVICE_STICKY=1
  ; git_branch.py only injects version for the default env; set it here.
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2
